# ---------------- Core ----------------
gradio>=5.45,<6
yt-dlp>=2025.9.5
requests>=2.32.0,<3
# HLS AES-128 解密加速：缺了它 yt-dlp 会退回纯 Python 解密，慢一个数量级
pycryptodomex>=3.20,<4

# ---------------- Backend API ----------------
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1

# ---------------- Playwright (解析直链 · 线路3) ----------------
playwright>=1.47,<2

# ---------------- 可选：如果你想用 httpx（当前代码未用到，可删）----------------
# httpx>=0.27,<0.28